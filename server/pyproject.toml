[project]
name = "jokero-blender-mcp"
version = "0.1.0"
description = "MCP server that bridges Claude and Blender for the Jokero project"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
